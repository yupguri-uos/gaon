APP_ENV=
SESSION_SECRET=

# DB (PostgreSQL + pgvector)
DATABASE_URL=

# Kakao OAuth
KAKAO_REST_API_KEY=
KAKAO_REDIRECT_URI=
KAKAO_CLIENT_SECRET=

# 객체 스토리지 (MinIO / S3 호환)
S3_ENDPOINT=
S3_ACCESS_KEY=
S3_SECRET_KEY=
S3_BUCKET=

# LLM
GOOGLE_API_KEY=
GEMINI_MODEL_FAST=
GEMINI_MODEL_QUALITY=
# 사용 안 함
ANTHROPIC_API_KEY=

# Embedding
EMBEDDING_MODEL=
# 빈 값이면 자동 선택(cuda → mps → cpu)
EMBEDDING_DEVICE=

# FCM (push)
FCM_PROJECT_ID=
FCM_CREDENTIALS_PATH=

# 포트 바인딩 대상 = 미니PC tailnet IP (인계 노트 기준). Tailscale IP가 바뀌면 여기만 수정.
TAILNET_IP=

# PostgreSQL
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=

# MinIO (root user/password — 8자 이상)
MINIO_ROOT_USER=
MINIO_ROOT_PASSWORD=

APP_ENV=
SESSION_SECRET=